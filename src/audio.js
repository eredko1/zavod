// Procedural Web Audio: weapons, ambience, footsteps, UI. Owned by: AUDIO agent. STUB — replace entirely.
export async function init(ctx) {
  return { play: () => {}, setListener: () => {} };
}
export function update(dt, ctx) {}
